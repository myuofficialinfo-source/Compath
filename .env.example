# Steam Compass設定ファイル
# このファイルを .env にコピーして、実際の値を設定してください

# Gemini API Key（サーバーサイドでのみ使用、クライアントには公開されません）
# https://aistudio.google.com/app/apikey から取得
GEMINI_API_KEY=your_gemini_api_key_here

# Steam API Key（オプション）
# https://steamcommunity.com/dev/apikey から取得
STEAM_API_KEY=your_steam_api_key_here

# サーバー設定
PORT=3000
NODE_ENV=development

# レート制限設定（1分あたりのリクエスト数）
RATE_LIMIT_MAX=30

# Upstash Redis（レート制限用）
# https://console.upstash.com から取得
# ツミナビと同じインスタンスを共有可能（prefixで分離）
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# 将来の有料化に向けた設定（現時点では未使用）
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# DATABASE_URL=
